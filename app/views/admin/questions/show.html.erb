<div class="flex flex-col w-8/12 mx-auto my-8">
  <h3 class="font-semibold text-2xl">Question de niveau <%= @question.level %>, catégorie <%= @question.category %></h3>
  <h4 class="my-8 text-xl"><%= @question.statement %> :</h4>
    <ul class="w-full italic text-justify text-lg">
      <li><%= @question.prop_one %></li>
      <li><%= @question.prop_two %></li>
      <li><%= @question.prop_three %></li>
    </ul>
  <h4 class="my-8 text-lg">La bonne réponse est la réponse <%= @question.answer.good_prop %>.</h4>
  <p class="text-justify text-lg"><%= @question.answer.explanation %>.</p>
  <div class="flex justify-evenly mt-8">
    <%= link_to "Modifier",  edit_admin_question_path(@question), class: "text-white bg-darkblue text-lg p-2 rounded" %>
    <%= link_to "Supprimer", admin_question_path(@question), data: {  turbo_method: :delete, turbo_confirm: "Are you sure?" }, class: "text-white bg-darkblue text-lg p-2 rounded" %>
  </div>
</div>

